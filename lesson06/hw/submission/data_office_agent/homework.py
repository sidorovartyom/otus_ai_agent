"""
Домашнее задание: Инструменты агента
Курс: Создание ИИ агентов, Урок 6

Выполнил: [Ваше ФИО]

Цель: Реализовать вызов внешнего инструмента и оформить логику его использования в виде SOP

Задание выполнено:
✅ 1. Определена функция для работы с SQL (generate_analytics_query)
✅ 2. Описана JSON-схема функции
✅ 3. Реализован вызов функции через LLM (реальный Anthropic API, с fallback на эмулятор)
✅ 4. Оформлен SOP для использования инструмента
✅ 5. Добавлена обработка базовых ошибок

Структура решения:
├── homework.py (этот файл) - основной файл для сдачи с JSON-схемой и SOP
├── db.py - работа с базой данных
├── db_schema.sql - DDL схема БД
├── db_test_data.sql - тестовые данные
├── analytics_tools.py - реализация инструмента generate_analytics_query
├── sql_generator.py - генерация параметризованного SQL запроса
├── dashboards.py - маппинг метрик на дашборды
├── runtime.py - регистрация инструментов, JSON-схема и валидация (источник истины для схемы)
├── real_llm.py - вызов инструмента через реальный Anthropic API
├── fake_llm.py - эмулятор LLM для запуска без .env/доступа к API
└── demo.py - демонстрация работы (реальный LLM с fallback на эмулятор)

Запуск: python -m data_office_agent.demo
"""

from __future__ import annotations

from .runtime import TOOLS

# ============================================================================
# 1. JSON-СХЕМА ИНСТРУМЕНТА
# ============================================================================
# Схема параметров хранится в runtime.py (там же, где выполняется валидация),
# чтобы не поддерживать две копии одной и той же схемы вручную. Здесь она
# просто оборачивается в формат, который ожидает Anthropic API (input_schema).

_TOOL_NAME = "generate_analytics_query"
_tool_def = TOOLS[_TOOL_NAME]

TOOL_SCHEMA = {
    "name": _TOOL_NAME,
    "description": _tool_def["description"],
    "input_schema": {
        "type": "object",
        "required": _tool_def["schema"]["required"],
        "properties": _tool_def["schema"]["properties"],
    },
}

# ============================================================================
# 2. SOP (Standard Operating Procedure)
# ============================================================================

SOP = """
═══════════════════════════════════════════════════════════════════════════════
                   SOP: generate_analytics_query
═══════════════════════════════════════════════════════════════════════════════

ЦЕЛЬ
────────────────────────────────────────────────────────────────────────────────
Помочь пользователю получить SQL запрос для анализа данных из Data Office
и предоставить ссылку на соответствующий дашборд для детального изучения.

КОГДА ИСПОЛЬЗОВАТЬ
────────────────────────────────────────────────────────────────────────────────
1. Пользователь запрашивает аналитические данные:
   • ЧП (cashflow) - чистая прибыль
   • Позиции/транзакции
   • Комиссии
   • Выручка
   • Информация о клиентах

2. Пользователь хочет фильтровать данные:
   • По каналу продаж (Digital, Retail, Corporate, Partner, Mobile App)
   • По сегменту клиентов (VIP, Premium, Standard, Small)
   • По периоду времени

3. Пользователь хочет получить ссылку на дашборд для детального анализа

ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ
────────────────────────────────────────────────────────────────────────────────
metric (строка):
  Какую метрику анализировать:
  • net_profit - ЧП (cashflow) из витрины fact_cashflow
  • positions - позиции/транзакции из витрины fact_positions
  • commissions - комиссии из витрины fact_commissions
  • revenue - выручка из витрины fact_cashflow
  • clients - информация о клиентах из dim_clients

period (строка):
  Период анализа:
  • Формат YYYY для анализа за год (например, "2026")
  • Формат YYYY-MM для анализа за месяц (например, "2026-06")
  • Диапазон лет: 2000-2100

ОПЦИОНАЛЬНЫЕ ПАРАМЕТРЫ
────────────────────────────────────────────────────────────────────────────────
sales_channel (строка):
  Фильтр по каналу продаж:
  • Digital - онлайн канал
  • Retail - розничный канал
  • Corporate - корпоративные продажи
  • Partner - партнерский канал
  • Mobile App - мобильное приложение

client_segment (строка):
  Фильтр по сегменту клиентов:
  • VIP - крупные клиенты (оборот > 1M)
  • Premium - средние клиенты (100K - 1M)
  • Standard - стандартные клиенты (10K - 100K)
  • Small - малые клиенты (< 10K)

group_by (строка):
  Группировка результатов:
  • channel - по каналам продаж
  • segment - по сегментам клиентов
  • client - по клиентам
  • month - по месяцам
  • none - без группировки (по умолчанию)

limit (целое число):
  Максимальное количество записей в результате
  • По умолчанию: 10, допустимый диапазон: 1-1000
  • Используйте меньше для "топ-N" запросов

ПРОЦЕСС ВЫПОЛНЕНИЯ
────────────────────────────────────────────────────────────────────────────────
1. Получить запрос от пользователя
2. Извлечь параметры (метрика, период, фильтры)
3. Валидировать параметры:
   - Проверить наличие обязательных полей (metric, period)
   - Проверить формат периода (YYYY или YYYY-MM)
   - Проверить допустимые значения enum (в т.ч. sales_channel, client_segment)
   - Проверить диапазон limit (1-1000)
4. Сгенерировать SQL запрос:
   - Выбрать таблицы в зависимости от метрики
   - Добавить JOIN для связи с справочниками
   - Подставить период/канал/сегмент/лимит как `?`-плейсхолдеры, а не как
     текст внутри SQL (params передаются отдельным списком)
   - При группировке заменить детальные колонки на агрегаты (SUM/COUNT),
     иначе результат по группе будет случайным, а не суммой
5. Выполнить запрос к БД (`conn.execute(sql, params)`) для проверки корректности
6. Определить соответствующий дашборд
7. Вернуть:
   - SQL запрос
   - Информацию о дашборде (название, URL, описание)
   - Превью результатов (первые 3 записи)
   - Общее количество записей

ОБРАБОТКА ОШИБОК
────────────────────────────────────────────────────────────────────────────────
validation_error:
  • Отсутствует обязательный параметр
  • Некорректный формат периода
  • Недопустимое значение параметра
  Действие: Вернуть сообщение с указанием проблемы

unknown_metric:
  • Запрошена неизвестная метрика
  Действие: Вернуть список допустимых метрик

sql_error:
  • Ошибка формирования SQL запроса
  Действие: Логировать ошибку, вернуть сообщение пользователю

database_error:
  • Ошибка выполнения запроса к БД
  Действие: Проверить подключение к БД, логировать ошибку

tool_error:
  • Непредвиденная ошибка выполнения
  Действие: Логировать полный trace, вернуть общее сообщение

БЕЗОПАСНОСТЬ
────────────────────────────────────────────────────────────────────────────────
✓ Read-only: Инструмент только читает данные, не изменяет их
✓ Параметризованные запросы: sql_generator.py возвращает SQL с `?`-плейсхолдерами
  и отдельный список params; значения period/sales_channel/client_segment/limit
  никогда не подставляются в текст запроса, а передаются в conn.execute(sql, params)
✓ Enum на уровне JSON-схемы: sales_channel и client_segment ограничены
  конкретным списком значений (см. runtime.py) - это доп. слой защиты
  до того, как значение вообще попадёт в SQL
✓ Валидация входных данных: типы, обязательные поля, формат периода, диапазон limit
✓ Ограничение LIMIT: жёсткий потолок 1000 записей независимо от того, что
  запросила модель/пользователь
✗ НЕ использует динамическую конкатенацию SQL со значениями параметров
✗ НЕ выполняет DDL/DML операции (CREATE, UPDATE, DELETE)

МАППИНГ НА ДАШБОРДЫ
────────────────────────────────────────────────────────────────────────────────
Метрика          → Дашборд
────────────────────────────────────────────────────────────────────────────────
net_profit       → Дашборд "ЧП"
                   https://bi.company.com/dashboards/cashflow
                   Детальный анализ чистой прибыли

positions        → Дашборд "Позиции"
                   https://bi.company.com/dashboards/positions
                   Анализ транзакций и продуктов

commissions      → Дашборд "Комиссии"
                   https://bi.company.com/dashboards/commissions
                   Анализ комиссионных выплат

revenue          → Дашборд "ЧП"
                   https://bi.company.com/dashboards/cashflow
                   Анализ выручки (входит в дашборд ЧП)

clients          → Дашборд "Клиенты"
                   https://bi.company.com/dashboards/clients
                   Анализ клиентской базы

ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
────────────────────────────────────────────────────────────────────────────────
Пример 1: Базовый запрос
  Запрос: "получить ЧП за текущий год по каналу продаж Digital"
  Параметры:
    {
      "metric": "net_profit",
      "period": "2026",
      "sales_channel": "Digital"
    }
  Результат:
    • SQL запрос с JOIN fact_cashflow + dim_contracts + dim_sales_channels
    • Фильтр WHERE period_date = 2026 AND channel_name = 'Digital'
    • Ссылка на Дашборд "ЧП"

Пример 2: Топ-N запрос
  Запрос: "показать топ-5 клиентов по позициям"
  Параметры:
    {
      "metric": "positions",
      "period": "2026",
      "group_by": "client",
      "limit": 5
    }
  Результат:
    • SQL запрос с GROUP BY client_name
    • ORDER BY total_amount DESC
    • LIMIT 5
    • Ссылка на Дашборд "Позиции"

Пример 3: Фильтр по сегменту
  Запрос: "комиссии за 2025 год по сегменту VIP"
  Параметры:
    {
      "metric": "commissions",
      "period": "2025",
      "client_segment": "VIP"
    }
  Результат:
    • SQL запрос с фильтром segment_name = 'VIP'
    • Ссылка на Дашборд "Комиссии"

Пример 4: Группировка по месяцам
  Запрос: "выручка по месяцам в 2026 году"
  Параметры:
    {
      "metric": "revenue",
      "period": "2026",
      "group_by": "month"
    }
  Результат:
    • SQL запрос с GROUP BY strftime('%Y-%m', period_date)
    • Помесячная разбивка
    • Ссылка на Дашборд "ЧП"

АРХИТЕКТУРА ДАННЫХ
────────────────────────────────────────────────────────────────────────────────
Справочники (Dimensions):
  • dim_sales_channels - каналы продаж
  • dim_client_segments - сегментация клиентов
  • dim_clients - клиенты
  • dim_contracts - договора

Факты (Facts):
  • fact_positions - позиции/транзакции
  • fact_cashflow - ЧП (cashflow) и выручка
  • fact_commissions - комиссии

Связи:
  fact_* → dim_contracts → dim_clients → dim_client_segments
  dim_contracts → dim_sales_channels

МОНИТОРИНГ И ОТЛАДКА
────────────────────────────────────────────────────────────────────────────────
При возникновении проблем проверить:
  1. Корректность формата периода
  2. Существование БД (data_office.db)
  3. Наличие данных за запрошенный период
  4. Корректность названий каналов и сегментов (регистр имеет значение)
  5. Логи выполнения SQL запросов

Для отладки:
  • Запустить demo.py для тестовых запросов
  • Проверить SQL запрос в SQLite браузере
  • Изучить превью результатов

═══════════════════════════════════════════════════════════════════════════════
Конец SOP
═══════════════════════════════════════════════════════════════════════════════
"""

# ============================================================================
# 3. ПРИМЕР ИНТЕГРАЦИИ
# ============================================================================

def example_usage():
    """Пример использования инструмента"""
    from .runtime import run_tool

    # Пример 1: Запрос ЧП по каналу Digital
    result1 = run_tool("generate_analytics_query", {
        "metric": "net_profit",
        "period": "2026",
        "sales_channel": "Digital"
    })

    # Пример 2: Топ-5 клиентов по позициям
    result2 = run_tool("generate_analytics_query", {
        "metric": "positions",
        "period": "2026",
        "group_by": "client",
        "limit": 5
    })

    return result1, result2


# ============================================================================
# 4. ДЕМОНСТРАЦИЯ
# ============================================================================

if __name__ == "__main__":
    import sys
    if sys.platform == 'win32':
        sys.stdout.reconfigure(encoding='utf-8')

    print(__doc__)
    print("\n" + "="*80)
    print("JSON-СХЕМА ИНСТРУМЕНТА")
    print("="*80)
    import json
    print(json.dumps(TOOL_SCHEMA, ensure_ascii=False, indent=2))

    print("\n" + "="*80)
    print("SOP (Standard Operating Procedure)")
    print("="*80)
    print(SOP)

    print("\n" + "="*80)
    print("Для запуска демонстрации выполните:")
    print("python -m data_office_agent.demo")
    print("="*80)
